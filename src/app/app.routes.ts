import { Routes } from '@angular/router';
import { MonitoringViewComponent } from './monitoring-view/monitoring-view.component';

export const routes: Routes = [
    {path: '', component: MonitoringViewComponent}
];
